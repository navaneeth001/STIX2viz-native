## What changed

<!-- A short summary, and the motivation behind it. -->

## Checklist

- [ ] `npm run verify` passes (lint, format, typecheck, tests, build, size budget, publint, attw)
- [ ] Prop compatibility with the `stix2vis` web package is preserved (or the change is deliberate and documented)
- [ ] No new runtime dependency (or the PR explains why it is unavoidable)
- [ ] The layout stays deterministic — no `Math.random()`, no wall-clock input
- [ ] Tests were added or updated, and intentional behaviour changes are called out below
- [ ] Documentation (`README.md`, `CHANGELOG.md`) updated where needed

## Behaviour changes

<!-- What existing callers would notice, if anything. -->

## Screenshots

<!-- Optional, but very helpful for anything that renders. -->
